{
  "name": "ai-email-assistant-frontend-tests",
  "private": true,
  "version": "0.0.0",
  "description": "Frontend safety-net tests for the AI Email Assistant UI (critical flows only).",
  "type": "module",
  "scripts": {
    "test": "node --test \"web/static/__tests__/**/*.test.mjs\""
  },
  "devDependencies": {
    "jsdom": "^25.0.1"
  }
}
